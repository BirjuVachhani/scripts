# Folders to exclude from flutterclean scanning
# Add one folder per line (bare name, or a pattern containing / to match a path)
# These are pruned, so they are never traversed at all.
.git
build
.dart_tool
Pods
.symlinks
ephemeral
node_modules
ios
android
macos
windows
linux
web
